//! Headless runner. Real implementation lands in M7; this stub keeps the
//! workspace shape fixed from M0.

fn main() {
    println!(
        "ripe-run {}: runner not implemented yet (see PLAN.md M7)",
        env!("CARGO_PKG_VERSION")
    );
}
