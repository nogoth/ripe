//! TUI editor. Real implementation lands in M8; this stub keeps the
//! workspace shape fixed from M0.

fn main() {
    println!(
        "ripe {}: TUI not implemented yet (see PLAN.md M8)",
        env!("CARGO_PKG_VERSION")
    );
}
